CONFIG_ZTEST=y
CONFIG_MAIN_STACK_SIZE=2048

# Logging (required by eai_log Zephyr backend)
CONFIG_LOG=y

# eai_log (required by wifi_prov)
CONFIG_EAI_LOG=y

# eai_settings (required by wifi_prov_cred)
CONFIG_EAI_SETTINGS=y

# Settings subsystem (uses "none" backend on QEMU — RAM only)
CONFIG_SETTINGS=y
CONFIG_SETTINGS_RUNTIME=y

# WiFi provisioning library
CONFIG_WIFI_PROV=y
CONFIG_WIFI_PROV_CRED=y
